# 同城寻缘雷达最短测试路径

这份文档只做一件事：用最短的路径确认 `同城寻缘雷达地图` 已经跑通。

## 开始前先确认

只看三件事：

1. `supabase_location_radar.sql` 已执行
2. 当前环境是 `localhost` 或 `https`
3. 项目已经重启过

如果这三项没满足，就先不要开始测。

## 最短测试准备

你至少需要两个账号：

- 账号 A：当前测试主账号
- 账号 B：附近异性账号

为了省时间，最简单的方法不是找两台设备真的站在一起，而是：

1. 先让两个账号都登录过一次
2. 都完成资料
3. 在 `profiles` 表里给两个人手动填接近的经纬度

推荐直接把两个人都先写成同一区域附近坐标，这样最快。

## 最短测试步骤

按这个顺序走：

1. 用账号 A 登录
2. 点击顶部 `同城雷达`
3. 允许浏览器定位
4. 确认地图正常显示
5. 在 `profiles` 表里确认账号 A 已写入 `latitude / longitude / location_updated_at`
6. 确认地图上出现账号 B 的头像 Marker
7. 点击账号 B 的头像
8. 确认底部弹出资料卡，并显示距离
9. 点击 `直接心动点赞`
10. 去 `likes` 表确认新增一条 `A -> B` 的 `like`

只要这 10 步通了，就说明这个功能主链路已经是通的。

## 最短通过标准

下面这些结果必须同时出现：

- 地图能打开
- 当前用户定位能写库
- 附近异性会被打点
- 点头像会弹资料卡
- 距离能显示
- 点赞能写入 `likes`

只要这 6 条成立，就可以认为第二个大功能已经具备演示和继续迭代的基础。

## 如果你还想多验一步

再补做这个动作：

1. 让账号 B 先对账号 A 点喜欢
2. 再回到地图页，用账号 A 点 `直接心动点赞`
3. 去 `matches` 表确认是否生成匹配

如果这一步也通，说明地图页已经和你原来的配对主链路打通了，而不是一个孤立功能。

## 最容易卡住的地方

如果最短测试没过，通常就卡在下面几种情况：

### 地图打不开

先查：

- 项目有没有重启
- 控制台里有没有底图加载报错

### 没有定位

先查：

- 当前是不是 `https` 或 `localhost`
- 浏览器是不是拒绝了定位权限
- 系统定位服务是不是关闭了

### 地图上没人

先查：

- 账号 B 是否真的有经纬度
- 账号 B 是否是异性
- 账号 B 是否在 5 公里内

### 点赞没写进去

先查：

- `likes` 表策略是否正常
- 当前用户是否已经登录
- 控制台或网络请求里有没有 Supabase 报错

## 我最推荐的实际测试法

如果你现在只想最快得到一个确定结果，就直接这样做：

1. 在数据库里手动把 A 和 B 的经纬度写得很接近
2. 用 A 登录进入地图页
3. 看 B 是否出现
4. 点 B 的头像
5. 点 `直接心动点赞`
6. 去数据库看 `likes`

这是整个功能最短、最稳、最不依赖真机环境的一条测试路径。
