# 心动配对 2 分钟讲稿

大家好，这个项目叫 `心动配对`，是我基于 `Vite + React + Supabase` 做的一套恋爱社交产品原型。

它不是只停留在页面展示，而是把一条核心主链路真正打通了：从注册登录、首次资料完善、推荐卡片浏览，到双向喜欢后的匹配、会话列表和即时聊天，整条流程都能实际跑起来。

这个项目的第一个重点，是我没有单纯做静态前端，而是接入了 `Supabase Auth`、数据库和 `Realtime`。也就是说，登录状态、资料信息、点赞关系、匹配结果和聊天消息，都是走真实数据，而不是本地写死的假数据。

第二个重点，是我把它做成了更像产品 Demo 的状态。比如推荐卡片不是普通列表，而是更接近真实交友产品的卡片流；会话列表支持最近消息排序和消息预览；聊天输入也支持 `Enter` 发送、`Shift + Enter` 换行，以及发送中的反馈气泡。

第三个重点，是这套项目已经上线了，而且我准备好了可直接演示的双账号环境。现在可以直接用两个测试账号登录，现场演示“女号发消息，男号重新登录后看到最新消息”这条链路，这说明匹配、会话和聊天都已经连通。

如果继续往产品方向扩展，下一步我会做头像上传、更完整的筛选和推荐逻辑、已读回执，以及更细的在线状态。

这个项目对我来说，最能体现的不是单个页面怎么写，而是我把一个从产品想法到可演示版本的完整前端体验真正做出来了。
