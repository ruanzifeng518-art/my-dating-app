# 心动配对项目卖点速记卡

## 一句话介绍

这是一个基于 `Vite + React + Supabase` 的恋爱社交产品原型，已经打通了登录、建档、推荐、匹配、会话和聊天的完整主链路，并且可以直接在线演示双账号收发消息。

## 30 秒版本

我这个项目不是只做了几个静态页面，而是把一条真实产品流程接起来了。用户可以注册登录、完善资料、浏览推荐卡片、完成双向匹配，再进入会话列表和聊天页。后端能力我用的是 `Supabase Auth + Database + Realtime`，所以资料、匹配和消息都是真实写库，而且项目已经部署上线，可以直接双账号演示。

## 60 秒版本

这个项目最核心的地方有三点。第一，我没有把它做成普通前端页面展示，而是把认证、数据库和实时消息接了起来，登录状态、资料信息、匹配关系和聊天消息都是真实数据。第二，我把产品体验做成了一条完整链路，从首次登录资料完善，到推荐卡片、双向喜欢、匹配成功、会话列表和即时聊天，整个流程是闭环的。第三，它不只是代码层面完成，而是已经能在线演示，我还准备了双测试账号，可以现场展示一端发消息、另一端收到消息的效果。

## 关键词速记

- `完整链路`
  从登录到聊天，不是单页展示
- `真实数据`
  `profiles / likes / matches / messages` 都走数据库
- `实时能力`
  聊天接了 `Supabase Realtime`
- `产品感`
  推荐卡片、会话列表、资料编辑、输入体验都做了打磨
- `可演示`
  已上线，双账号已验证

## 你最该强调的点

1. 我做的不是静态原型，而是能真正跑起来的交互式产品 Demo。
2. 我把页面、状态、数据库和实时消息串成了一条完整体验链路。
3. 这个项目已经具备现场演示条件，而不是只停留在本地开发环境。

## 常见追问短答

### 这个项目最大的技术点是什么

不是某一个单独页面，而是把 `Auth + Database + Realtime` 接进前端流程，让登录、资料、匹配和聊天能形成闭环。

### 为什么用 Supabase

因为这个项目重点是快速验证完整产品链路。用 `Supabase` 可以把认证、数据库和实时消息一次接通，让我把精力更多放在产品体验和前端交互上。

### 这个项目最像真实产品的地方是什么

不是只有推荐卡片，而是做了会话列表、资料编辑、聊天输入优化和双账号可验证消息链路，所以更接近真正可使用的产品原型。

### 如果继续做，下一步做什么

我会优先补头像上传、更完整的推荐筛选逻辑、已读回执和在线状态，把它从课程级 Demo 继续推进到更完整的产品原型。

## 临场别忘了说

- 已经上线
- 有现成测试账号
- 双账号消息链路已验证
- 不是纯前端假数据切换
- 可以现场操作给别人看
