# 演示指南

这份文档用于帮助你在答辩、面试、路演或给别人看项目时，更顺畅地演示这套“心动配对”原型。

## 当前可直接演示的线上版本

当前线上地址：

- `https://ruanzifeng518-art.github.io/my-dating-app/`

这版已经验证可直接演示以下主链路：

1. 登录
2. 资料查看与编辑
3. 推荐卡片浏览
4. 会话列表
5. 进入聊天页
6. 一端发送消息，另一端重新登录后看到新消息

## 演示前准备

如果你要现场演示，建议提前确认：

1. 线上地址可以正常打开
2. 两个测试账号都能正常登录
3. `profiles / likes / matches / messages` 表都已可用
4. 会话列表里能看到预置的匹配关系
5. 演示前不要再随意删除这两组演示数据

## 已验证测试账号

这套项目目前已经准备好两组可直接演示的测试账号：

- 男号：`qa20260611app+001@example.com` / `Test123456`
- 女号：`qa20260611app+002@example.com` / `Test123456`

当前这两组账号已验证的状态：

- 两边都能正常登录
- 两边资料已完善
- 两边已建立匹配关系
- 会话列表可直接看到对方
- 聊天记录里已有预置消息
- 女号新发出的消息，男号重新登录后已验证可以看到

如果你不想在公开仓库里长期保留这组账号，可以在正式对外前自行替换；但在当前演示阶段，这两组账号可以直接用。

## 推荐演示顺序

### 1. 登录

先演示：

- 打开线上地址
- 用男号登录一次
- 成功进入主界面

建议讲法：

“这个项目不是静态页面，而是接了 Supabase Auth。登录后系统会判断用户有没有资料，再决定进入引导页还是主界面。”

### 2. 资料编辑

接着点顶部 `编辑资料`：

- 修改昵称
- 修改简介
- 修改兴趣标签
- 保存

重点展示：

- 保存后顶部资料立即更新
- 资料写回 `profiles` 表

建议讲法：

“用户不是只能第一次建档，登录后也可以继续修改自己的展示资料，这样体验更像真实产品。”

### 3. 会话列表

从顶部点 `会话列表`，可以直接展示：

- 已存在的匹配关系
- 最近消息排序
- 最新消息预览
- 点击后直接进入聊天

建议讲法：

“我没有把聊天入口做成一次性的匹配弹窗，而是补成了真正可持续使用的会话列表，所以这个项目更接近完整产品原型。”

### 4. 聊天页

进入聊天页后，演示：

- 已有历史消息
- 输入一条新消息
- `Enter` 发送
- 输入框自动清空
- 返回后会话列表预览更新

建议讲法：

“聊天不是静态演示，而是已经接了数据库和实时消息链路，消息写入后会直接出现在会话里。”

### 5. 双账号切换演示

如果你想演示“对端收到消息”，建议按这个顺序：

1. 先用女号登录
2. 进入与男号的聊天页
3. 发一条新消息
4. 退出登录
5. 再用男号登录
6. 打开 `会话列表`
7. 展示男号已经能看到女号刚发来的新消息

这是当前最稳的一条演示路径，因为这条链路已经实际验证通过。

### 6. 推荐卡片

回到推荐卡片页，演示：

- 用户头像和资料展示
- 契合度和共同兴趣
- 左滑无感
- 右滑心动

重点展示：

- 卡片信息不是纯假 UI，而是来自 `profiles`
- 交互上已经具备产品 Demo 的完成度

建议讲法：

“这里我把推荐卡片做成了更像正式产品的样子，不只是一个列表，而是带有信息层级、契合度和共同兴趣提示的卡片流。”

### 7. 双向匹配

当前环境里，演示账号已经提前准备好双向 like 和 match 数据。

所以更推荐的展示方式是：

- 直接讲清楚匹配机制
- 从会话列表进入聊天，证明匹配关系已经存在
- 如果需要，再补充展示卡片页的喜欢 / 无感写库逻辑

演示重点：

- 双向喜欢后触发匹配
- 匹配成功弹窗
- 可以直接进入聊天页

建议讲法：

“匹配不是前端假弹窗，而是由 likes 数据推动 matches 生成，再回到前端做反馈。”

## 最适合强调的点

如果你时间不多，优先强调这 5 件事：

1. 登录和资料完善不是静态流程，而是接了真实认证和数据库
2. 推荐卡片来自真实资料，不是纯前端硬编码
3. 双向喜欢后会生成匹配关系
4. 会话列表支持最近消息排序和消息预览
5. 两个账号间的收发链路已经在线上验证通过

## 3 分钟演示版本

如果你只有 3 分钟，可以按这个顺序：

1. 男号登录
2. 展示顶部资料和编辑资料
3. 打开会话列表
4. 进入聊天页
5. 切到女号发一条消息

## 5 分钟演示版本

如果你有 5 分钟，可以按这个顺序：

1. 男号登录
2. 编辑资料并保存
3. 展示推荐卡片和共同兴趣
4. 说明 likes / matches 机制
5. 切到女号发送一条消息
6. 切回男号打开会话列表和聊天页

## 常见问题

### 会话列表是空的怎么办？

先检查：

- `matches` 表里是否已经生成对应关系
- 当前登录账号是否就是匹配双方之一
- `messages` 表是否能根据 `match_id` 查到消息
- 相关查询策略是否已经执行完成

### 没有推荐卡片怎么办？

先检查：

- `profiles` 表里是否有异性资料
- 当前用户是否已完成资料
- 是否已经把可显示的对象都滑完了

### 匹配弹窗没出现怎么办？

先检查：

- `likes` 是否确实双向写入
- `matches` 是否已生成
- 相关 SQL 策略是否执行完成

### 聊天没有消息怎么办？

先检查：

- `messages` 表是否能正常写入
- 当前会话的 `match_id` 是否正确
- 当前账号是否确实属于这条 `match`

### 两个账号之间如何最快演示？

推荐直接按这个顺序：

1. 女号登录
2. 打开和男号的聊天
3. 发一条消息
4. 退出登录
5. 男号登录
6. 打开会话列表
7. 点进会话展示新消息

## 建议的仓库展示方式

如果你要把这个项目作为作品集展示，建议在仓库首页同时配上：

- 一张登录页截图
- 一张推荐卡片截图
- 一张聊天页截图
- README 中的功能清单
- 这份 `DEMO_GUIDE.md`

这样别人既能看代码，也知道该怎么体验这套原型。
