# 演示指南

这份文档用于帮助你在答辩、面试、路演或给别人看项目时，更顺畅地演示这套“心动配对”原型。

## 演示前准备

建议提前准备：

1. 一个已配置好的 Supabase 项目
2. 至少 2 个测试账号
3. 至少 2 条不同性别的 `profiles` 数据
4. 已执行仓库内的 SQL 脚本
5. 本地前端已能正常运行

建议先确认：

- `npm run dev` 可以正常启动
- 登录页可访问
- 测试账号可以正常登录
- `profiles / likes / matches / messages` 表都已可用

## 建议的测试账号准备

出于安全考虑，不建议把公开可用的测试账号密码直接写进仓库。

你可以自己在 Supabase Auth 中准备两类账号：

- 账号 A：用于主演示视角
- 账号 B：用于配对与聊天对端

推荐准备方式：

- A 和 B 性别不同，便于推荐卡片直接出现
- 两个账号都提前完善好资料
- 头像、昵称、简介、兴趣标签尽量有区分度

## 推荐演示顺序

### 1. 登录

先演示：

- 打开登录页
- 输入测试账号
- 成功进入主界面

建议讲法：

“这个项目不是静态页面，而是接了 Supabase Auth。登录后系统会判断用户有没有完善过资料，再决定进入引导页还是主界面。”

### 2. 资料编辑

接着点顶部 `编辑资料`：

- 修改昵称
- 修改简介
- 修改兴趣标签
- 保存

重点展示：

- 保存后顶部资料立即更新
- 资料写回 `profiles` 表

建议讲法：

“用户不是只能第一次建档，登录后也可以继续修改自己的展示资料，这样体验更像真实产品。”

### 3. 推荐卡片

回到推荐卡片页，演示：

- 用户头像和资料展示
- 契合度和共同兴趣
- 左滑无感
- 右滑心动

重点展示：

- 卡片信息不是纯假 UI，而是来自 `profiles`
- 交互上已经具备产品 Demo 的完成度

建议讲法：

“这里我把推荐卡片做成了更像正式产品的样子，不只是一个列表，而是带有信息层级、契合度和共同兴趣提示的卡片流。”

### 4. 双向匹配

要演示这一段时，可以让另一个测试账号也给当前账号点一次喜欢，或者提前准备好双向 like 数据。

演示重点：

- 双向喜欢后触发匹配
- 匹配成功弹窗
- 可以直接进入聊天页

建议讲法：

“匹配不是前端假弹窗，而是由 likes 数据推动 matches 生成，再回到前端做反馈。”

### 5. 会话列表

从顶部点 `会话列表`，展示：

- 最近消息排序
- 未读数
- 活跃时间
- 可从列表直接进入聊天

建议讲法：

“我把聊天入口从一次性弹窗，扩展成了可持续使用的会话列表，这样产品状态就更完整了。”

### 6. 实时聊天

进入聊天页后，演示：

- 发送消息
- `Enter` 直接发送
- `Shift + Enter` 换行
- 发送中反馈
- 对端发消息后实时刷新

建议讲法：

“聊天页接了 Supabase Realtime，不需要刷新页面就能收到新消息，这也是这个项目最核心的一条体验链路。”

## 最适合强调的点

如果你时间不多，优先强调这 5 件事：

1. 登录和资料完善不是静态流程，而是接了真实认证和数据库
2. 推荐卡片来自真实资料，不是纯前端硬编码
3. 双向喜欢后会生成匹配关系
4. 会话列表支持最近消息排序和未读状态
5. 聊天支持实时同步

## 3 分钟演示版本

如果你只有 3 分钟，可以按这个顺序：

1. 登录
2. 展示顶部资料和编辑资料
3. 展示推荐卡片
4. 进入会话列表
5. 打开聊天页并发送一条消息

## 5 分钟演示版本

如果你有 5 分钟，可以按这个顺序：

1. 登录
2. 编辑资料并保存
3. 展示推荐卡片和共同兴趣
4. 说明双向匹配机制
5. 打开会话列表
6. 进入聊天页并演示实时消息

## 常见问题

### 没有推荐卡片怎么办？

先检查：

- `profiles` 表里是否有异性资料
- 当前用户是否已完成资料
- 是否已经把可显示的对象都滑完了

### 匹配弹窗没出现怎么办？

先检查：

- `likes` 是否确实双向写入
- `matches` 是否已生成
- 相关 SQL 策略是否执行完成

### 聊天没有实时更新怎么办？

先检查：

- `messages` 表是否能正常写入
- Realtime 是否已对相关表开启
- 当前会话的 `match_id` 是否正确

## 建议的仓库展示方式

如果你要把这个项目作为作品集展示，建议在仓库首页同时配上：

- 一张登录页截图
- 一张推荐卡片截图
- 一张聊天页截图
- README 中的功能清单
- 这份 `DEMO_GUIDE.md`

这样别人既能看代码，也知道该怎么体验这套原型。
