# 同城寻缘雷达真实联调指导

这份文档不是部署说明，也不是验收清单。它只解决一件事：你现在要真正把 `同城寻缘雷达地图` 跑起来，应该怎么一步一步联调。

## 目标

联调成功的标准只有这一条：

用账号 A 登录后，进入 `同城雷达`，能在地图上看到账号 B 的头像，点开资料卡后还能直接完成 `心动点赞`。

## 联调前先准备两个人

至少准备两个账号：

- 账号 A：当前测试主账号
- 账号 B：附近异性账号

这两个账号都需要：

1. 已注册
2. 已登录过
3. 已完成资料
4. 在 `profiles` 表里有一条自己的记录

如果账号本身都还没写进 `profiles`，地图功能就没法测。

## 第一步：确认数据库字段已经存在

先在 `Supabase SQL Editor` 执行：

- `supabase_location_radar.sql`

执行完以后，去 `profiles` 表确认已经有这三个字段：

- `latitude`
- `longitude`
- `location_updated_at`

只要这一步没做，后面所有定位写库都会失败。

## 第二步：先把地图本身跑起来

现在项目默认使用公开暗色底图，所以这里不再强制要求你先配 `Mapbox Token`。

你只需要先重新启动：

```bash
npm run dev
```

接着登录账号 A，点击顶部 `同城雷达`。

如果地图本身都打不开，不要急着查数据库，先查：

1. 项目有没有重启
2. 控制台里有没有底图加载相关报错
3. 网络环境是否拦截了公开底图资源

## 第三步：先验证账号 A 定位能写库

进入地图页后，浏览器会请求定位权限。

你需要：

1. 点击允许
2. 等地图加载
3. 回到 `Supabase profiles` 表
4. 找到账号 A 对应的那一行

确认：

- `latitude` 已写入
- `longitude` 已写入
- `location_updated_at` 已更新

如果账号 A 这里都写不进去，就先不要继续查地图上为什么没人。

## 第四步：手动给账号 B 造一组附近坐标

真实定位联调会受设备、网络、浏览器权限影响，最省时间的方法是直接在数据库里把账号 B 改到账号 A 附近。

做法：

1. 在 `profiles` 表里找到账号 A 的 `latitude / longitude`
2. 把账号 B 的经纬度改成非常接近的一组值

例如：

- 账号 A：`31.2304 / 121.4737`
- 账号 B：`31.2311 / 121.4745`

只要两组值足够近，通常都会落在 5 公里内。

同时确认：

- 账号 B 的 `gender` 和账号 A 的筛选逻辑匹配
- 账号 B 不是当前登录账号自己

## 第五步：回到地图页刷新雷达

回到页面后：

1. 点击 `刷新我的定位`
2. 等地图重新加载
3. 看地图上有没有出现账号 B 的头像 Marker

如果没有出现，优先只查这四件事：

1. 账号 B 是否真的有经纬度
2. 账号 B 是否是异性
3. 账号 B 是否已经写入 `profiles`
4. 账号 B 和账号 A 的距离是否真的在 5 公里内

## 第六步：验证点击头像和资料卡

地图上看到账号 B 以后，点一下头像 Marker。

你应该看到：

- 底部弹出资料卡
- 显示昵称
- 显示年龄
- 显示简介
- 显示兴趣标签
- 显示“距离你 x.xkm”

如果点了头像没反应，先查：

1. 页面控制台是否报错
2. Marker 是否被别的浮层遮挡
3. 账号 B 的对象结构里是否缺少必要字段

## 第七步：验证直接心动点赞

资料卡打开以后，点击：

- `直接心动点赞`

然后去数据库看：

- `likes` 表里是否新增一条 `A -> B`

如果有，就说明地图页已经打通了原来的点赞链路。

如果没有，优先查：

1. `likes` 表权限策略
2. 当前登录账号是否正确
3. 控制台或网络请求是否有 Supabase 错误

## 第八步：再验匹配联动

如果你想继续确认地图页不只是写 `likes`，还真能打通匹配逻辑，就补做一步：

1. 先让账号 B 对账号 A 写一条 `like`
2. 再回到账号 A 的地图页点 `直接心动点赞`
3. 去 `matches` 表确认是否生成匹配记录

如果这一步也通，就说明地图功能已经和你原来的配对主链路接上了。

## 我最推荐的联调顺序

如果你现在就要最快看到成果，直接按这个顺序：

1. 执行 `supabase_location_radar.sql`
2. 登录账号 A
3. 打开 `同城雷达`
4. 允许定位
5. 确认账号 A 的经纬度写进 `profiles`
6. 手动把账号 B 改到账号 A 附近
7. 回到页面刷新雷达
8. 点击账号 B 头像
9. 点击 `直接心动点赞`
10. 去 `likes` 表确认结果

这就是目前最稳的一条真实联调路径。

## 如果你联调时卡住了

最常见的卡点只有这几类：

### 地图打不开

先看项目是否重启，以及底图资源是否加载失败。

### 没有定位

先看浏览器权限、系统定位和 `https`。

### 地图上没人

先看账号 B 的性别、经纬度和距离。

### 点赞没写进去

先看 `likes` 表策略和 Supabase 报错。

### 距离显示不合理

先看数据库里写进去的经纬度是不是乱填了。

## 联调完成后你至少应该能确认

到最后，至少要看到这几个事实：

- 账号 A 定位能写入数据库
- 账号 B 能被地图打点出来
- 点击头像能展开资料卡
- 距离会显示
- 点赞能写入 `likes`
- 双向喜欢时能继续联动 `matches`
