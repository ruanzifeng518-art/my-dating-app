# 同城寻缘雷达地图部署说明

这份文档只解决一件事：把 `同城寻缘雷达地图` 安全、完整地跑起来。

## 先说你要准备什么

这个功能依赖三样东西：

1. `Supabase profiles` 表里要有经纬度字段
2. 前端要配置 `Mapbox Token`
3. 浏览器或手机要允许定位权限

只要这三项满足，地图就能打开，用户定位就能写库，附近 5 公里内的异性用户就能在地图上显示出来。

## 第一步：执行 Supabase SQL

先在 `Supabase SQL Editor` 里执行：

- `supabase_location_radar.sql`

它会做三件事：

1. 给 `profiles` 表增加 `latitude`
2. 给 `profiles` 表增加 `longitude`
3. 给 `profiles` 表增加 `location_updated_at`

同时还会补两个索引，方便后续查找和排序。

如果你已经执行过一次，也可以重复执行，因为脚本里用了 `if not exists`。

## 第二步：配置 Mapbox Token

你需要去 [Mapbox](https://www.mapbox.com/) 注册或登录账号，然后创建一个公开访问令牌。

项目里需要的是：

- `VITE_MAPBOX_TOKEN`

你可以在本地 `.env` 里填写：

```bash
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
VITE_MAPBOX_TOKEN=YOUR_PUBLIC_MAPBOX_TOKEN
```

如果你是部署到线上，也要在对应平台的环境变量里加上同样这条：

- `VITE_MAPBOX_TOKEN`

这个令牌是前端公开令牌，可以放到前端环境变量里，但建议你去 `Mapbox` 后台限制允许访问的域名，避免被别人滥用。

## 第三步：重启前端

本地开发时，配置完 `.env` 以后要重新启动：

```bash
npm run dev
```

如果不重启，新的环境变量不会生效。

## 第四步：怎么验证功能正常

登录之后，按这个顺序验证：

1. 点击顶部 `同城雷达`
2. 浏览器弹出定位授权
3. 点击允许
4. 地图加载完成
5. 当前用户位置出现在地图上
6. 附近 5 公里内的异性用户以头像 Marker 显示
7. 点击某个头像
8. 底部出现资料卡，显示距离和 `直接心动点赞`

如果这些都正常，说明功能已经跑通。

## 这个功能现在会把什么写进数据库

当用户允许定位后，当前账号会把以下数据写进 `profiles`：

- `latitude`
- `longitude`
- `location_updated_at`

所以后续你可以继续基于这些字段做：

- 同城推荐
- 附近活动
- 距离排序
- 更精细的城市或区域筛选

## 为什么有时定位拿不到

最常见的原因有这几个。

### 1. 不是 HTTPS

浏览器定位通常只在这两种环境下工作：

- `localhost`
- `https`

如果你是在普通 `http` 环境访问，很多浏览器会直接拒绝定位。

### 2. 用户拒绝了定位权限

如果第一次点了拒绝，后面浏览器可能不会再自动弹框。

这时需要去浏览器站点权限里手动改回“允许位置访问”。

### 3. 设备本身没开定位

如果是手机或笔记本，系统层面的 GPS / 定位服务没开，浏览器也拿不到位置。

### 4. 桌面浏览器定位不够准

桌面设备常常是根据 IP 或 Wi-Fi 做粗略定位，不一定非常精确。

如果你想更接近真实效果，建议用手机浏览器测试。

## 为什么地图上可能看不到别人

这通常不是地图坏了，而是数据条件还不满足。

优先检查：

1. 别的用户资料里有没有写入 `latitude` 和 `longitude`
2. 对方性别是否和当前用户的筛选逻辑匹配
3. 对方距离是否真的在 5 公里内
4. 对方是否已经完成资料，能正常出现在 `profiles` 表中

也就是说，这个功能需要至少两类用户都写入了定位，地图上才会比较热闹。

## 推荐测试方式

最省事的测试方法是：

1. 用两个测试账号登录
2. 都允许定位
3. 保证两台设备实际位置接近，或者手动在数据库里填接近的经纬度
4. 回到地图页刷新定位

这样更容易看到彼此出现在地图雷达里。

## 当前功能已经包含什么

这次仓库里已经完成：

- `MapRadar.jsx` 地图页
- 浏览器定位写库
- 头像 Marker 打点
- 呼吸灯光晕动画
- 点击头像展开底部资料卡
- 显示距离
- `直接心动点赞`
- 与现有 `likes / matches` 链路打通

## 你现在只要做的事

1. 在 `Supabase` 执行 `supabase_location_radar.sql`
2. 配置 `VITE_MAPBOX_TOKEN`
3. 重启项目或重新部署
4. 登录后点击顶部 `同城雷达`
