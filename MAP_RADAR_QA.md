# 同城寻缘雷达地图验收清单

这份文档用于上线前确认 `同城寻缘雷达地图` 是否已经真正可用。

## 基础配置

先确认这几项已经准备好：

- 已执行 `supabase_location_radar.sql`
- 项目重新启动或重新部署过
- 当前环境支持 `https` 或 `localhost`

如果这几项没完成，后面的验证会直接失败。

## 数据库检查

进入 `Supabase Table Editor` 或 `SQL Editor`，确认 `profiles` 表已经有这三个字段：

- `latitude`
- `longitude`
- `location_updated_at`

如果字段不存在，地图功能不会有真实位置数据可读写。

## 定位权限检查

用浏览器或手机打开项目后，确认：

1. 点击顶部 `同城雷达`
2. 浏览器会弹出定位权限
3. 用户点击允许

验收通过标准：

- 页面没有报错
- 没有一直卡在加载中
- 当前用户位置能被识别

如果这里失败，优先检查浏览器站点权限和设备系统定位开关。

## 写库检查

允许定位后，回到 `Supabase profiles` 表确认当前登录账号这一行已经更新：

- `latitude` 不是空
- `longitude` 不是空
- `location_updated_at` 有最新时间

验收通过标准：

- 三个字段都已经写入
- 刷新页面后仍然能读取到这组数据

## 地图渲染检查

进入 `同城雷达` 页后，确认：

- 地图能正常显示
- 不是白屏
- 不是空白容器
- 缩放控件可以显示
- 当前用户位置有自己的光点标记

验收通过标准：

- 地图可拖动
- 地图可缩放
- 当前用户位置 Marker 可见

## 附近用户打点检查

准备至少一个异性测试账号，并保证对方 `profiles` 里也有经纬度。

然后确认：

1. 对方距离在 5 公里内
2. 对方性别符合当前筛选逻辑
3. 地图上能出现头像 Marker
4. Marker 有呼吸光晕效果

验收通过标准：

- 5 公里内的异性会被打点
- 超出 5 公里的不会显示
- 同性不会显示

## 点击头像交互检查

在地图上点某个头像 Marker，确认：

- 底部弹出微型资料卡
- 能看到昵称
- 能看到年龄
- 能看到简介
- 能看到兴趣标签
- 能看到“距离你 x.xkm”

验收通过标准：

- 点击不同头像时，底部卡片能切换
- 点击地图空白处后，卡片能收起

## 距离显示检查

确认距离显示逻辑是合理的：

- 小于 1 公里时，用米显示
- 大于等于 1 公里时，用公里显示
- 数据不会出现 `NaN`
- 数据不会出现明显负值或异常值

验收通过标准：

- 距离文案与两点位置大致一致
- 没有乱码、空白或错误数字

## 直接心动点赞检查

点开资料卡以后，点击 `直接心动点赞`，确认：

1. `likes` 表里新增当前用户对目标用户的 `like`
2. 按钮状态会更新
3. 页面不会报错

验收通过标准：

- `likes` 表写入成功
- 按钮不会重复无限点击
- 界面上能看到“已发送心动”之类的状态变化

## 匹配联动检查

如果对方之前已经对当前用户点过喜欢，再执行一次地图里的 `直接心动点赞`，确认：

1. `matches` 表新增对应记录
2. 页面提示匹配成功

验收通过标准：

- `matches` 确实建成
- 后续会话链路不受影响

## 刷新与重进检查

确认以下场景也正常：

- 刷新页面后还能进入地图页
- 再次点击 `同城雷达` 不会崩
- 再次请求定位不会卡死
- 已写库经纬度可以被重新读取

验收通过标准：

- 功能不是一次性成功，而是可以稳定重复进入

## 真机检查

推荐至少做一次手机真机测试。

重点确认：

- 手机浏览器能拿到更准确定位
- 地图拖动和缩放流畅
- Marker 点击区域正常
- 底部资料卡在小屏上不会挡住主要操作

验收通过标准：

- 手机端可用
- 没有明显布局错位

## 常见失败原因

如果地图功能异常，优先看下面这些地方：

1. `supabase_location_radar.sql` 没执行
2. 浏览器拒绝了定位
3. 当前环境不是 `https` 或 `localhost`
4. 附近异性账号本身没有经纬度
5. 对方距离其实超过 5 公里

## 最终通过标准

上线前，至少要满足这几条：

- 当前用户定位能成功写入数据库
- 地图能正常渲染
- 5 公里内异性用户能正确打点
- 点击头像能弹出资料卡
- 距离显示正确
- 直接心动点赞能写入 `likes`
- 双向喜欢时能联动生成 `matches`
- 刷新后仍能稳定使用
