# 心动配对 5 分钟讲稿

大家好，我这个项目叫 `心动配对`，它是一个基于 `Vite + React + Supabase` 做的恋爱社交产品原型。这个项目我想解决的，不是只把页面画出来，而是把一条真正能演示、能操作、能讲清楚的数据链路做完整。

我先说这个项目最终做到的效果。现在它已经打通了注册登录、首次资料完善、推荐卡片浏览、喜欢和无感、双向匹配、会话列表、资料编辑，以及即时聊天这整条主流程。也就是说，它不是一个单页面作品，而是更接近一个小型产品 Demo。

这个项目的第一层重点，是我把真实后端能力接进来了。我没有自己搭传统服务端，而是直接用 `Supabase Auth` 管登录，用数据库表去存 `profiles`、`likes`、`matches` 和 `messages`，再用 `Realtime` 去处理聊天消息的同步。所以登录状态、资料数据、配对关系和聊天记录都是真实写库，不是前端假数据切换。

第二层重点，是产品流转被我做成了完整体验。比如用户第一次登录，不会直接看到推荐卡片，而是先进入资料完善流程，把头像、昵称、性别、年龄、简介和兴趣标签补齐，再写入 `profiles`。资料完成以后，系统才会进入推荐卡片页，去读取异性资料并做过滤，避免把已经点过喜欢或无感的人重复推给用户。

第三层重点，是匹配和聊天这条链路。我在前端把“喜欢”和“无感”都做成明确动作，并把 `likes` 数据写入数据库。双向喜欢之后，会生成 `matches` 关系，前端就可以通过 `matches` 和 `messages` 去构建会话列表。也就是说，会话列表并不是静态写出来的，而是由配对关系和消息表反推出来的，这样它才更像真实产品。

第四层重点，是交互细节。我把聊天页做了一些更接近真实应用的优化，比如 `Enter` 直接发送，`Shift + Enter` 换行，输入框会自动增高，发送过程中有临时气泡反馈，会话列表会根据最新消息排序，也能显示消息预览。这些点单独看不算很大，但组合起来之后，整个 Demo 的完成度会明显更高。

第五层重点，是演示可用性。我现在已经把项目部署到了 `GitHub Pages`，而且准备好了两组可直接演示的账号。现场可以直接登录男号和女号，演示一端发消息、另一端重新登录后在会话列表里看到新消息。这一点很重要，因为它说明这个项目不是“看起来像能用”，而是已经到了“可以现场操作给别人看”的状态。

如果继续往下做，我下一步会优先补三个方向。第一是头像上传和更完整的资料系统，让用户内容更真实。第二是更细的推荐和筛选逻辑，让匹配不只是按性别过滤。第三是已读回执、在线状态和更完整的聊天体验，把它从 Demo 继续推向更完整的产品原型。

如果让我用一句话总结这个项目，我会说：这不是一个只展示页面的前端练习，而是一个把认证、资料、匹配、会话和聊天真正串起来的可演示产品原型。
